import { ComingSoon } from "@/components/coming-soon";

export default function SettingsPage() {
  return (
    <ComingSoon
      title="Settings"
      description="Library-wide settings (default seat fee, seat count adjustments, admin password change) will live here."
    />
  );
}
